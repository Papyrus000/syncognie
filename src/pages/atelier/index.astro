---
import { getCollection } from 'astro:content';
import AtelierLayout from '../../layouts/AtelierLayout.astro';
import { PENSEURS, DOMAINES } from '../../data/penseurs';
import type { Domaine } from '../../data/penseurs';

const tout = await getCollection('atelier');

const master = tout.find(e => e.id === 'index.md' || e.slug === 'index');
const entrees = tout
  .filter(e => e.id !== 'index.md' && e.slug !== 'index' && e.data.draft !== true)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

if (!master) throw new Error('Fichier src/content/atelier/index.md introuvable');
const { Content } = await master.render();

// Progression globale — basée sur mode 2 (detail), pas sur article rédigé
const exploresIds = new Set(tout.filter(e => e.data.penseur).map(e => e.data.penseur));
const total    = PENSEURS.length;
const explores = PENSEURS.filter(p => !!p.detail).length;
const pct      = Math.round((explores / total) * 100);

// ── Niveau 1 · Soi — détail par domaine ─────────────────────────────────────
const domainesSoi: Domaine[] = ['cerveau', 'construction', 'relations', 'sens'];

const niv1Domaines = domainesSoi.map(id => {
  const { label, couleur } = DOMAINES[id];
  const liste    = PENSEURS.filter(p => p.domaine === id);
  const explored = liste.filter(p => !!p.detail).length;
  const penseurs = liste.map(p => ({
    id: p.id,
    titre: `${p.nom} — ${p.concept}`,
    explored: !!p.detail,
    slug: p.id,
  }));
  return { id, label, couleur, explored, total: liste.length, penseurs };
});

const niv1Total    = niv1Domaines.reduce((s, d) => s + d.total, 0);
const niv1Explored = niv1Domaines.reduce((s, d) => s + d.explored, 0);

// ── Niveau 1b · Corps — détail par domaine ───────────────────────────────────
const domainesCorps: Domaine[] = ['corps-sommeil', 'corps-mouvement', 'corps-metabolisme'];

const niv1bDomaines = domainesCorps.map(id => {
  const { label, couleur } = DOMAINES[id];
  const liste    = PENSEURS.filter(p => p.domaine === id);
  const explored = liste.filter(p => !!p.detail).length;
  const penseurs = liste.map(p => ({
    id: p.id,
    titre: `${p.nom} — ${p.concept}`,
    explored: !!p.detail,
    slug: p.id,
  }));
  return { id, label, couleur, explored, total: liste.length, penseurs };
});

const niv1bTotal    = niv1bDomaines.reduce((s, d) => s + d.total, 0);
const niv1bExplored = niv1bDomaines.reduce((s, d) => s + d.explored, 0);

// ── Niveaux 2 & 3 — barre simple ─────────────────────────────────────────────
const niveauxSimples = [
  { num: 2, label: 'Le monde',      couleur: '#5090a8', couleurBorder: 'rgba(80,144,168,0.35)' },
  { num: 3, label: 'Comment vivre', couleur: '#8a9e5a', couleurBorder: 'rgba(138,158,90,0.35)' },
];

const niveauxSimplesData = niveauxSimples.map(niv => {
  const liste    = PENSEURS.filter(p => p.niveau === niv.num);
  const explored = liste.filter(p => !!p.detail).length;
  const penseurs = liste.map(p => ({
    id: p.id,
    titre: `${p.nom} — ${p.concept}`,
    explored: !!p.detail,
    slug: p.id,
  }));
  return { ...niv, explored, total: liste.length, penseurs };
});

function formatDate(date: Date) {
  return date.toLocaleDateString('fr-FR', { day: 'numeric', month: 'short', year: 'numeric' });
}

// ── Sections non finalisées — mettre à true pour réactiver ──────────────────
const SHOW_NIVEAU_0  = false;  // /atelier/le-reel
const SHOW_NIVEAU_4  = false;  // /atelier/agir
const SHOW_SYNTHESE  = false;  // /atelier/synthese
---

<AtelierLayout
  title={`${master.data.title} — L'Atelier des Idées`}
  description="L'Atelier des Idées : explorations de philosophie, psychologie, neurosciences et systèmes complexes. Des articles de fond sur les penseurs qui comptent vraiment."
  ogTitle="L'Atelier des Idées — Syncognie"
  ogDescription="Philosophie, psychologie, neurosciences, systèmes : des explorations de fond sur les penseurs et idées qui éclairent comment fonctionne le monde — et comment y vivre."
  filename={master.data.title}
>

  <!-- ── Hero unifié : image + texte overlay ── -->
  <div class="atelier-hero-stage">
  <div class="atelier-hero-wrap">
    <picture>
      <source
        srcset="/images/atelier-hero-v3.webp"
        type="image/webp"
      />
      <source
        srcset="/images/atelier-hero-v3.jpg"
        type="image/jpeg"
      />
      <img
        src="/images/atelier-hero-v3.jpg"
        alt="Syncognie — l'Atelier des Idées, illustration pixel art"
        width="1400"
        height="764"
        loading="eager"
        decoding="async"
        class="atelier-hero-img"
      />
    </picture>
    <!-- Texte superposé -->

  </div>
  <!-- Ombre sol -->
  <div class="atelier-hero-ground"></div>
  </div>

  <!-- ── Terminal line ── -->
  <div class="atelier-terminal">
    <span class="atelier-terminal-prompt">&gt;</span>
    <span class="atelier-terminal-text" id="terminalText"></span><span class="atelier-terminal-cursor" id="terminalCursor">_</span>
  </div>

  <!-- Niveau 0 : SHOW_NIVEAU_0 = false pour désactiver, true pour réactiver -->
  {SHOW_NIVEAU_0 && (
  <a href="/atelier/le-reel" class="extra-bloc extra-bloc--reel" id="reelBloc" aria-label="Voir la page Le Réel">
    <div class="extra-bloc-inner">
      <div class="extra-titre-ligne">
        <span class="extra-badge" style="color:#9b6fd4;border-color:rgba(155,111,212,0.35)">niveau 0</span>
        <span class="extra-titre" style="color:#c8b4f0">Le Réel</span>
        <span class="extra-desc">substrat physique · thermodynamique · évolution · information</span>
        <span class="extra-arrow" style="color:#9b6fd4">&#x2192;</span>
      </div>
      <div class="extra-domaines">
        <span class="extra-dom" style="--c:#9b6fd4">entropie</span>
        <span class="extra-dom-sep">·</span>
        <span class="extra-dom" style="--c:#5daa88">évolution darwinienne</span>
        <span class="extra-dom-sep">·</span>
        <span class="extra-dom" style="--c:#5d9ed4">Shannon &amp; Kolmogorov</span>
        <span class="extra-dom-sep">·</span>
        <span class="extra-dom" style="--c:#c47b40">émergence</span>
      </div>
    </div>
    <div class="extra-glow extra-glow--reel"></div>
  </a>
  )}

  <!-- ── Bloc Fondations ── -->
  <div class="fondations-bloc" id="fondationsBloc">

    <!-- Ligne principale : titre + label + flèche vers page complète -->
    <a href="/atelier/fondations" class="fondations-main" aria-label="Voir la page Fondations complete">
      <span class="fondations-titre">Fondations</span>
      <span class="fondations-label">Base</span>
      <span class="fondations-cta">
        <span class="fondations-cta-text">voir la page</span>
        <span class="fondations-arrow">&#x2192;</span>
      </span>
    </a>

    <!-- Barre de progression globale -->
    <a href="/atelier/fondations" class="fondations-meta" aria-label="Voir toutes les fondations">
      <div class="fondations-bar">
        <div class="fondations-fill" style={`width: ${pct}%`}></div>
      </div>
      <span class="fondations-compte">{explores} / {total}</span>
    </a>

    <!-- ── Niveau 1 · Soi — avec sous-domaines ── -->
    <div class="fniv-bloc">
      <div class="fniv-bloc-head">
        <span class="fniv-bloc-badge">niveau 1</span>
        <span class="fniv-bloc-titre">Soi</span>
        <span class="fniv-bloc-compte">{niv1Explored}/{niv1Total}</span>
      </div>
      <div class="fniv-sous-liste">
        {niv1Domaines.map(d => (
          <div class="fniv-sous" style={`--c: ${d.couleur}`}>
            <span class="fniv-sous-label">{d.label}</span>
            <div class="fniv-cases">
              {d.penseurs.map((p) => (
                <a
                  href={`/atelier/fondations#open=${p.id}`}
                  class={`fniv-case fniv-case--link${p.explored ? ' fniv-case--on' : ''}`}
                  data-title={p.explored ? p.titre : 'Article à venir'}
                  aria-label={p.explored ? p.titre : 'À venir'}
                  onClick="event.stopPropagation()"
                ></a>
              ))}
            </div>
            <span class="fniv-sous-compte">{d.explored}/{d.total}</span>
          </div>
        ))}
      </div>
    </div>

    <!-- ── Niveau 1b · Corps — avec sous-domaines ── -->
    <div class="fniv-bloc fniv-bloc--corps">
      <div class="fniv-bloc-head">
        <span class="fniv-bloc-badge fniv-bloc-badge--corps">niveau 1 corps</span>
        <span class="fniv-bloc-titre">Le corps</span>
        <span class="fniv-bloc-compte fniv-bloc-compte--corps">{niv1bExplored}/{niv1bTotal}</span>
      </div>
      <div class="fniv-sous-liste">
        {niv1bDomaines.map(d => (
          <div class="fniv-sous" style={`--c: ${d.couleur}`}>
            <span class="fniv-sous-label">{d.label}</span>
            <div class="fniv-cases">
              {d.penseurs.map((p) => (
                <a
                  href={`/atelier/fondations#open=${p.id}`}
                  class={`fniv-case fniv-case--link${p.explored ? ' fniv-case--on' : ''}`}
                  data-title={p.explored ? p.titre : 'Article à venir'}
                  aria-label={p.explored ? p.titre : 'À venir'}
                  onClick="event.stopPropagation()"
                ></a>
              ))}
            </div>
            <span class="fniv-sous-compte">{d.explored}/{d.total}</span>
          </div>
        ))}
      </div>
    </div>

    <!-- ── Niveaux 2 & 3 — barres simples ── -->
    <div class="fondations-niveaux">
      {niveauxSimplesData.map(niv => (
        <div class="fniv" style={`--c: ${niv.couleur}; --border: ${niv.couleurBorder}`}>
          <span class="fniv-badge-num">niveau {niv.num}</span>
          <span class="fniv-label">{niv.label}</span>
          <div class="fniv-cases">
            {niv.penseurs.map((p) => (
              <a
                href={`/atelier/fondations#open=${p.id}`}
                class={`fniv-case fniv-case--link${p.explored ? ' fniv-case--on' : ''}`}
                data-title={p.explored ? p.titre : 'Article à venir'}
                aria-label={p.explored ? p.titre : 'À venir'}
                onClick="event.stopPropagation()"
              ></a>
            ))}
          </div>
          <span class="fniv-compte">{niv.explored}/{niv.total}</span>
        </div>
      ))}
    </div>

  </div>


  <!-- Niveau 4 : SHOW_NIVEAU_4 = false pour désactiver, true pour réactiver -->
  {SHOW_NIVEAU_4 && (
  <a href="/atelier/agir" class="extra-bloc extra-bloc--agir" id="agirBloc" aria-label="Voir la page Agir">
    <div class="extra-bloc-inner">
      <div class="extra-titre-ligne">
        <span class="extra-badge" style="color:#c87d3c;border-color:rgba(200,125,60,0.35)">niveau 4</span>
        <span class="extra-titre" style="color:#f0b870">Agir</span>
        <span class="extra-desc">décision · jeu · stratégie · coopération</span>
        <span class="extra-arrow" style="color:#c87d3c">&#x2192;</span>
      </div>
      <div class="extra-domaines">
        <span class="extra-dom" style="--c:#c87d3c">Bayes · Kahneman · Taleb</span>
        <span class="extra-dom-sep">·</span>
        <span class="extra-dom" style="--c:#b85a4a">Nash · Schelling</span>
        <span class="extra-dom-sep">·</span>
        <span class="extra-dom" style="--c:#8a6840">Clausewitz</span>
        <span class="extra-dom-sep">·</span>
        <span class="extra-dom" style="--c:#6a9860">Axelrod · Ostrom</span>
      </div>
    </div>
    <div class="extra-glow extra-glow--agir"></div>
  </a>
  )}

  <!-- Synthèse : SHOW_SYNTHESE = false pour désactiver, true pour réactiver -->
  {SHOW_SYNTHESE && (
  <a href="/atelier/synthese" class="extra-bloc extra-bloc--synthese" aria-label="Voir la synthèse — boîte à outils">
    <div class="extra-bloc-inner">
      <div class="extra-titre-ligne">
        <span class="extra-badge" style="color:#7abd6e;border-color:rgba(122,189,110,0.35)">synthèse</span>
        <span class="extra-titre" style="color:#b0e090">Boîte à outils</span>
        <span class="extra-desc">35 penseurs condensés · connexions · index pratique</span>
        <span class="extra-arrow" style="color:#7abd6e">&#x2192;</span>
      </div>
      <div class="extra-domaines">
        <span class="extra-dom" style="--c:#f5a623">Cerveau</span>
        <span class="extra-dom-sep">·</span>
        <span class="extra-dom" style="--c:#e85d20">Construction</span>
        <span class="extra-dom-sep">·</span>
        <span class="extra-dom" style="--c:#d45a8a">Relations</span>
        <span class="extra-dom-sep">·</span>
        <span class="extra-dom" style="--c:#5090a8">Systèmes</span>
        <span class="extra-dom-sep">·</span>
        <span class="extra-dom" style="--c:#8a9e5a">Philosophie</span>
      </div>
    </div>
    <div class="extra-glow extra-glow--synthese"></div>
  </a>
  )}

  <!-- Tooltip global -->
  <div class="fniv-tooltip" id="fnivTooltip" aria-hidden="true"></div>




<!-- Lien Fondations étendues (ex-Socle) -->
<a href="/atelier/socle" class="socle-fondations-bloc" aria-label="Fondations étendues">
  <span class="socle-fondations-titre">Fondations étendues</span>
  <span class="socle-fondations-label">Extension</span>
  <span class="socle-fondations-cta">
    <span class="socle-fondations-cta-text">voir la page</span>
    <span class="socle-fondations-arrow">&#x2192;</span>
  </span>
</a>



<!-- ── Outils Interactifs ── -->

<div class="outils-system">

  <div class="outils-system-head">
    <span class="outils-system-label">système</span>
    <span class="outils-system-dot">·</span>
    <span class="outils-system-name">Granularité Émotionnelle</span>
    <span class="outils-system-count">2 outils</span>
  </div>

  <div class="outils-duo">

    <!-- Outil 1 : Évaluer -->
    <a href="/atelier/granularite-emotionnelle" class="outil-duo-bloc outil-duo-bloc--evaluer">
      <div class="outil-duo-step">01 · évaluer</div>
      <div class="outil-duo-titre">Granularité Émotionnelle</div>
      <p class="outil-duo-desc">Nommez précisément ce que vous ressentez. Stoppe le piratage de l'amygdale en 5 étapes.</p>
      <span class="outil-duo-arrow">&#x2192;</span>
      <div class="outil-duo-glow outil-duo-glow--evaluer"></div>
    </a>

    <!-- Connecteur visuel -->
    <div class="outil-duo-connector" aria-hidden="true">
      <div class="outil-duo-connector-line"></div>
      <span class="outil-duo-connector-icon">&#x2192;</span>
      <div class="outil-duo-connector-line"></div>
    </div>

    <!-- Outil 2 : Entraîner -->
    <a href="/atelier/nuancier" class="outil-duo-bloc outil-duo-bloc--entrainer">
      <div class="outil-duo-step">02 · entraîner</div>
      <div class="outil-duo-titre">Le Nuancier Émotionnel</div>
      <p class="outil-duo-desc">10 scénarios, 4 mots, 1 nuance juste. Entraînez votre cerveau à différencier des émotions très proches.</p>
      <span class="outil-duo-arrow">&#x2192;</span>
      <div class="outil-duo-glow outil-duo-glow--entrainer"></div>
    </a>

  </div>

</div>


  <!-- ── Lien discret : Le Salon des Idées ── -->
  <div class="salon-lien-wrap">
    <a href="/atelier/dialogue" class="salon-lien">
      <span class="salon-lien-dot"></span>
      <span class="salon-lien-label">Le Salon des Idées</span>
      <span class="salon-lien-sub">dialogues entre penseurs · prochainement</span>
      <span class="salon-lien-arrow">&#x2192;</span>
    </a>
  </div>

  <!-- ── Articles ── -->
  {entrees.length > 0 && (
    <div class="atelier-entrees">
      <div class="entrees-sep"></div>
      <h2>Exploration</h2>
      <ul class="entrees-list">
        {entrees.map((entree) => (
          <li>
            <a href={`/atelier/${entree.slug}`} class="entree-link">
              <span class="entree-arrow">&#x2192;</span>
              <span class="entree-title">{entree.data.title}</span>
              {entree.data.extrait && (
                <span class="entree-extrait">{entree.data.extrait}</span>
              )}
              <span class="entree-date">{formatDate(entree.data.date)}</span>
            </a>
          </li>
        ))}
      </ul>
    </div>
  )}

</AtelierLayout>

<script>
  // ── Terminal typewriter ──────────────────────────────────────────────────
  const termText   = document.getElementById('terminalText');
  const termCursor = document.getElementById('terminalCursor');
  const phrase     = "espace de concepts, d'idées, de penseurs";

  if (termText && termCursor) {
    let i = 0;
    termCursor.style.opacity = '0';

    const type = () => {
      if (i < phrase.length) {
        termText.textContent += phrase[i];
        i++;
        setTimeout(type, 28 + Math.random() * 22);
      } else {
        // frappe terminée — curseur se met à clignoter
        termCursor.style.opacity = '1';
      }
    };

    // légère pause avant de commencer
    setTimeout(type, 520);
  }

  const tooltip = document.getElementById('fnivTooltip');
  const cases   = document.querySelectorAll<HTMLElement>('.fniv-case[data-title]');

  if (tooltip) {
    let hideTimer: ReturnType<typeof setTimeout> | null = null;

    cases.forEach(el => {
      el.addEventListener('mouseenter', () => {
        const title = el.dataset.title;
        if (!title) return;
        if (hideTimer) { clearTimeout(hideTimer); hideTimer = null; }
        tooltip.textContent = title;
        tooltip.classList.add('fniv-tooltip--visible');
        tooltip.classList.toggle('fniv-tooltip--venir', title === 'Article a venir');
        const rect = el.getBoundingClientRect();
        tooltip.style.left = `${rect.left + rect.width / 2}px`;
        tooltip.style.top  = `${rect.top - 8}px`;
      });
      el.addEventListener('mouseleave', () => {
        hideTimer = setTimeout(() => {
          tooltip.classList.remove('fniv-tooltip--visible');
        }, 120);
      });
    });
  }
</script>

<style>
  /* ── Bloc Fondations ── */
  .fondations-bloc {
    display: block;
    text-decoration: none;
    border: 1px solid rgba(232, 160, 80, 0.18);
    background: rgba(62, 38, 16, 0.45);
    border-radius: 12px;
    padding: 1.2rem 1.5rem 1.4rem;
    margin: 1.8rem 0 1.8rem;
    position: relative;
    overflow: hidden;
    transition: border-color 0.25s, background 0.25s, transform 0.2s, box-shadow 0.25s;
  }
  .fondations-bloc::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 3px;
    background: linear-gradient(90deg, #e85d20, #f5a623, #ffe066, #f5a623, #e85d20);
    opacity: 0.55;
    transition: opacity 0.25s;
  }
  .fondations-bloc:hover {
    border-color: rgba(232, 160, 80, 0.32);
    background: rgba(72, 46, 20, 0.58);
    transform: translateY(-2px);
    box-shadow: 0 8px 28px rgba(20, 8, 0, 0.4);
  }
  .fondations-bloc:hover::before { opacity: 1; }

  /* Ligne titre — lien cliquable */
  .fondations-main {
    display: flex;
    align-items: baseline;
    gap: 0.75rem;
    margin-bottom: 1rem;
    text-decoration: none;
    width: 100%;
    max-width: 100%;
  }
  .fondations-titre {
    font-family: 'Libre Baskerville', serif;
    font-size: 1.1rem;
    font-weight: 700;
    font-style: italic;
    color: #e8a050;
    letter-spacing: 0.01em;
    flex-shrink: 0;
  }
  .fondations-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.58rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.16);
    flex: 1;
  }
  .fondations-cta {
    display: flex;
    align-items: center;
    gap: 0.3rem;
    flex-shrink: 0;
  }
  .fondations-cta-text {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.70rem;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    color: rgba(232, 160, 80, 0.55);
    opacity: 0;
    transform: translateX(-4px);
    transition: opacity 0.2s, transform 0.2s;
  }
  .fondations-arrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 1.4rem;
    color: #e8a050;
    opacity: 0.45;
    flex-shrink: 0;
    transition: opacity 0.2s, transform 0.2s;
  }
  .fondations-main:hover .fondations-cta-text { opacity: 1; transform: translateX(0); }
  .fondations-main:hover .fondations-arrow    { opacity: 1; transform: translateX(3px); }

  /* Barre globale */
  .fondations-meta {
    display: flex;
    align-items: center;
    gap: 0.9rem;
    margin-bottom: 1.4rem;
    text-decoration: none;
    cursor: pointer;
  }
  .fondations-meta:hover .fondations-fill {
    box-shadow: 0 0 10px rgba(245,166,35,0.7);
  }
  .fondations-meta:hover .fondations-compte {
    color: #f5a623;
    opacity: 1;
  }
  .fondations-bar {
    flex: 1;
    height: 4px;
    background: rgba(255,255,255,0.06);
    border-radius: 4px;
    overflow: hidden;
  }
  .fondations-fill {
    height: 100%;
    background: linear-gradient(90deg, #e85d20, #f5a623, #ffe066);
    border-radius: 4px;
    box-shadow: 0 0 6px rgba(245,166,35,0.45);
  }
  .fondations-compte {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.65rem;
    color: #f5a623;
    white-space: nowrap;
  }

  /* Blocs niveaux */
  .fniv-bloc {
    margin-bottom: 1.1rem;
    padding: 0.85rem 1rem;
    border-radius: 8px;
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.06);
  }
  .fniv-bloc--corps {
    background: rgba(123,159,212,0.04);
    border-color: rgba(123,159,212,0.12);
  }
  .fniv-bloc-head {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    margin-bottom: 0.75rem;
  }
  .fniv-bloc-badge {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.55rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(245,166,35,0.55);
    flex-shrink: 0;
  }
  .fniv-bloc-badge--corps { color: rgba(123,159,212,0.6); }
  .fniv-bloc-titre {
    font-family: 'Libre Baskerville', serif;
    font-size: 0.82rem;
    font-style: italic;
    font-weight: 700;
    color: rgba(240,210,160,0.75);
    flex: 1;
  }
  .fniv-bloc-compte {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.6rem;
    color: rgba(245,166,35,0.5);
    white-space: nowrap;
  }
  .fniv-bloc-compte--corps { color: rgba(123,159,212,0.55); }

  /* Sous-domaines */
  .fniv-sous-liste { display: flex; flex-direction: column; gap: 0.45rem; }
  .fniv-sous { display: flex; align-items: center; gap: 0.65rem; }
  .fniv-sous-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.6rem;
    color: var(--c);
    opacity: 0.9;
    white-space: nowrap;
    width: 8.5rem;
    flex-shrink: 0;
    letter-spacing: 0.02em;
  }
  .fniv-sous-compte {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.58rem;
    color: var(--c);
    opacity: 0.5;
    white-space: nowrap;
    width: 2.2rem;
    text-align: right;
    flex-shrink: 0;
  }

  /* Cases */
  .fniv-cases { display: flex; flex-wrap: wrap; gap: 3px; flex: 1; }
  .fniv-case {
    width: 10px;
    height: 10px;
    border-radius: 2px;
    border: 1px solid rgba(255,255,255,0.12);
    background: transparent;
    transition: background 0.25s, box-shadow 0.25s, transform 0.15s;
    flex-shrink: 0;
    cursor: default;
  }
  .fniv-case--on {
    background: var(--c);
    border-color: var(--c);
    box-shadow: 0 0 5px var(--c);
  }
  .fniv-case--link {
    cursor: pointer;
    text-decoration: none;
    display: inline-block;
    position: relative;
  }
  .fniv-case--link:hover {
    transform: scale(1.35);
    box-shadow: 0 0 9px var(--c), 0 0 18px rgba(255,255,255,0.15);
    z-index: 2;
  }

  /* Niveaux 2 & 3 */
  .fondations-niveaux { display: flex; flex-direction: column; gap: 0.5rem; margin-top: 0.2rem; }
  .fniv { display: flex; align-items: center; gap: 0.65rem; }
  .fniv-badge-num {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.52rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--c);
    opacity: 0.5;
    white-space: nowrap;
    width: 4rem;
    flex-shrink: 0;
  }
  .fniv-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.62rem;
    color: var(--c);
    opacity: 0.85;
    white-space: nowrap;
    width: 7rem;
    flex-shrink: 0;
    letter-spacing: 0.03em;
  }
  .fniv-compte {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.6rem;
    color: var(--c);
    opacity: 0.6;
    white-space: nowrap;
    width: 2.5rem;
    text-align: right;
    flex-shrink: 0;
  }

  /* Tooltip */
  .fniv-tooltip {
    position: fixed;
    pointer-events: none;
    z-index: 9999;
    transform: translate(-50%, -100%);
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.68rem;
    letter-spacing: 0.03em;
    line-height: 1.4;
    background: rgba(30, 14, 4, 0.94);
    color: #f0c880;
    border: 1px solid rgba(232, 160, 60, 0.28);
    border-radius: 6px;
    padding: 5px 10px;
    white-space: nowrap;
    box-shadow: 0 4px 16px rgba(0,0,0,0.4);
    opacity: 0;
    transition: opacity 0.15s ease;
  }
  .fniv-tooltip::after {
    content: '';
    position: absolute;
    bottom: -5px;
    left: 50%;
    width: 8px;
    height: 8px;
    background: rgba(30, 14, 4, 0.94);
    border-right: 1px solid rgba(232, 160, 60, 0.28);
    border-bottom: 1px solid rgba(232, 160, 60, 0.28);
    transform: translateX(-50%) rotate(45deg);
  }
  .fniv-tooltip--visible { opacity: 1; }
  .fniv-tooltip--venir { color: rgba(180, 140, 90, 0.55); font-style: italic; }

  /* ── Hero stage (conteneur perspective) ── */
  .atelier-hero-stage {
    margin: 0 auto 0.8rem;
    width: 85%;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  /* Ombre sol : ellipse aplatie sous l'image */
  .atelier-hero-ground {
    width: 70%;
    height: 12px;
    background: radial-gradient(ellipse at center,
      rgba(0,0,0,0.28) 0%,
      rgba(0,0,0,0.10) 45%,
      transparent 75%
    );
    border-radius: 50%;
    margin-top: -4px;
    filter: blur(3px);
    flex-shrink: 0;
  }
  /* ── Hero unifié ── */
  .atelier-hero-wrap {
    margin: 0;
    border-radius: 10px;
    overflow: hidden;
    border: 1px solid rgba(232, 160, 60, 0.18);
    box-shadow:
      0 4px 32px rgba(0, 0, 0, 0.55),
      inset 0 1px 0 rgba(255, 200, 100, 0.07);
    width: 100%;
    position: relative;
    /* ── Surélevé statique ── */
    transform: translateY(-4px);
  }
  /* Vignette 4 bords */
  .atelier-hero-wrap::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 10px;
    box-shadow: inset 0 0 70px 20px rgba(14, 5, 1, 0.65);
    z-index: 1;
    pointer-events: none;
  }
  /* Fondu bas profond pour accueillir le texte */
.atelier-hero-wrap::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 55%;
  background: linear-gradient(
    to bottom,
    transparent 0%,
    rgba(22, 8, 2, 0.2) 45%,    /* 0.6 → 0.2 */
    rgba(18, 6, 1, 0.55) 100%   /* 0.92 → 0.55 */
  );
  border-radius: 0 0 10px 10px;
  z-index: 2;
  pointer-events: none;
}
  .atelier-hero-img {
    width: 100%;
    height: auto;
    display: block;
    image-rendering: pixelated;
    image-rendering: crisp-edges;
  }
  /* ── Terminal line ── */
  .atelier-terminal {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
    margin: 0 auto 2rem;
    width: 85%;
    padding: 0 0.2rem;
  }
  .atelier-terminal-prompt {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.82rem;
    color: #7abd6e;
    flex-shrink: 0;
    user-select: none;
  }
  .atelier-terminal-text {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.82rem;
    color: rgba(212, 168, 100, 0.75);
    letter-spacing: 0.02em;
    font-style: italic;
  }
  .atelier-terminal-cursor {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.82rem;
    color: #7abd6e;
    animation: blink 1.1s step-end infinite;
    opacity: 0;
  }
  @keyframes blink {
    0%, 100% { opacity: 1; }
    50%       { opacity: 0; }
  }
  /* ── Mobile portrait (≤ 480px) ── */
  @media (max-width: 480px) {
    .atelier-terminal { width: 100%; padding: 0; }
    .atelier-terminal-text,
    .atelier-terminal-prompt,
    .atelier-terminal-cursor { font-size: 0.68rem; }
    /* Hero plus grand en portrait */
    .atelier-hero-stage { width: 100%; }
    .atelier-terminal   { width: 100%; }
    /* Fondations : labels plus courts */
    .fniv-sous-label { width: 5rem; font-size: 0.55rem; }
    .fniv-label      { width: 4.5rem; font-size: 0.55rem; }
    .fniv-badge-num  { display: none; }
    .fniv-case       { width: 8px; height: 8px; }
    /* CTA toujours visible sur touch (pas de hover fiable) */
    .fondations-cta-text,
    .socle-fondations-cta-text { display: none; }
    /* Flèche toujours visible */
    .fondations-arrow,
    .socle-fondations-arrow { opacity: 0.7; }
    /* Listes d'articles : extrait tronqué */
    .entree-extrait  { display: none; }
    .entree-link     { flex-wrap: wrap; gap: 0.4rem; }
    .entree-date     { margin-left: 0; font-size: 0.65rem; }
    .entree-title    { font-size: 0.82rem; }
    /* Bloc fondations : padding réduit */
    .fondations-bloc { padding: 1rem 0.9rem 1.1rem; }
  }

  /* ── Mobile paysage + petites tablettes (481px – 768px) ── */
  @media (min-width: 481px) and (max-width: 768px) {
    .atelier-terminal { width: 100%; padding: 0; }
    .atelier-terminal-text,
    .atelier-terminal-prompt,
    .atelier-terminal-cursor { font-size: 0.75rem; }
    .atelier-hero-stage { width: 92%; }
    .fniv-sous-label { width: 7rem; }
    .fniv-label      { width: 5.5rem; }
    .fniv-badge-num  { display: none; }
    .fniv-case       { width: 9px; height: 9px; }
    /* CTA text masqué mais flèche visible */
    .fondations-cta-text,
    .socle-fondations-cta-text { display: none; }
    .fondations-arrow,
    .socle-fondations-arrow { opacity: 0.7; }
    .entree-extrait  { max-width: 140px; }
  }

  /* Articles */
  .atelier-entrees { margin-top: 2.5rem; }
  .entrees-sep { border-top: 1px solid rgba(255,150,80,0.12); margin-bottom: 1.8rem; }
  .atelier-entrees h2 {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.95rem;
    font-weight: 500;
    color: #c87830;
    margin-bottom: 1.2rem;
  }
  .entrees-list { list-style: none; padding: 0; display: flex; flex-direction: column; gap: 0.6rem; }
  .entree-link {
    display: flex;
    align-items: baseline;
    gap: 0.7rem;
    text-decoration: none;
    padding: 0.5rem 0.6rem;
    border-radius: 6px;
    transition: background 0.2s;
  }
  .entree-arrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.85rem;
    color: #7a5a3a;
    flex-shrink: 0;
    transition: color 0.2s, transform 0.2s;
  }
  .entree-link:hover .entree-arrow { color: #7abd6e; transform: translateX(3px); }
  .entree-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.9rem;
    color: #d4a878;
    flex-shrink: 0;
    transition: color 0.2s;
  }
  .entree-link:hover .entree-title { color: #7abd6e; }
  .entree-extrait {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.78rem;
    color: #7a5a3a;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    flex: 1;
  }
  .entree-date {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.72rem;
    color: #5a3a1a;
    flex-shrink: 0;
    margin-left: auto;
  }

  /* Breakpoint interne index — supprimé (doublon couvert par le bloc ci-dessus) */

  /* ── Blocs Niveau 0 et Niveau 4 ── */
  .extra-bloc {
    display: block;
    text-decoration: none;
    border-radius: 10px;
    padding: 0.85rem 1.2rem;
    margin: 0.7rem 0;
    position: relative;
    overflow: hidden;
    transition:
      border-color 0.25s,
      background 0.25s,
      transform 0.2s,
      box-shadow 0.25s;
  }
  .extra-bloc--reel {
    background: rgba(50, 32, 72, 0.42);
    border: 1px solid rgba(155,111,212,0.22);
  }
  .extra-bloc--agir {
    background: rgba(62, 36, 14, 0.48);
    border: 1px solid rgba(200,125,60,0.2);
  }
  .extra-bloc::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    opacity: 0.55;
    transition: opacity 0.25s;
  }
  .extra-bloc--reel::before {
    background: linear-gradient(90deg, #9b6fd4, #5d9ed4, #5daa88, #9b6fd4);
  }
  .extra-bloc--agir::before {
    background: linear-gradient(90deg, #c87d3c, #b85a4a, #8a6840, #6a9860, #c87d3c);
  }
  .extra-bloc--reel:hover {
    border-color: rgba(155,111,212,0.45);
    background: rgba(70, 44, 100, 0.55);
    transform: translateY(-2px);
    box-shadow: 0 6px 24px rgba(155,111,212,0.15);
  }
  .extra-bloc--agir:hover {
    border-color: rgba(200,125,60,0.42);
    background: rgba(80, 50, 18, 0.6);
    transform: translateY(-2px);
    box-shadow: 0 6px 24px rgba(200,125,60,0.13);
  }
  .extra-bloc:hover::before { opacity: 0.9; }

  .extra-glow {
    position: absolute;
    bottom: -30px;
    right: -30px;
    width: 120px;
    height: 120px;
    border-radius: 50%;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.35s ease;
    filter: blur(35px);
  }
  .extra-glow--reel  { background: #9b6fd4; }
  .extra-glow--agir  { background: #c87d3c; }
  .extra-bloc:hover .extra-glow { opacity: 0.15; }

  .extra-bloc-inner { position: relative; z-index: 1; }

  .extra-titre-ligne {
    display: flex;
    align-items: baseline;
    gap: 0.6rem;
    margin-bottom: 0.5rem;
    flex-wrap: wrap;
  }
  .extra-badge {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.52rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    border: 1px solid;
    padding: 2px 7px;
    border-radius: 3px;
    flex-shrink: 0;
    opacity: 0.8;
  }
  .extra-titre {
    font-family: 'Libre Baskerville', serif;
    font-size: 1rem;
    font-weight: 700;
    font-style: italic;
    letter-spacing: 0.01em;
    flex-shrink: 0;
  }
  .extra-desc {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.58rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.15);
    flex: 1;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .extra-arrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 1.1rem;
    opacity: 0.35;
    flex-shrink: 0;
    transition: opacity 0.2s, transform 0.2s;
  }
  .extra-bloc:hover .extra-arrow {
    opacity: 1;
    transform: translateX(3px);
  }

  .extra-domaines {
    display: flex;
    align-items: center;
    gap: 0.35rem;
    flex-wrap: wrap;
  }
  .extra-dom {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.6rem;
    color: var(--c);
    opacity: 0.7;
    letter-spacing: 0.03em;
  }
  .extra-dom-sep {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.6rem;
    color: rgba(255,255,255,0.12);
  }

  @media (max-width: 480px) {
    .extra-desc { display: none; }
    .extra-titre { font-size: 0.88rem; }
  }

  /* ── Bloc Synthèse ── */
  .extra-bloc--synthese {
    background: rgba(20, 38, 18, 0.42);
    border: 1px solid rgba(122,189,110,0.18);
  }
  .extra-bloc--synthese::before {
    background: linear-gradient(90deg, #7abd6e, #5daa88, #e8b832, #5090a8, #7abd6e);
  }
  .extra-bloc--synthese:hover {
    border-color: rgba(122,189,110,0.4);
    background: rgba(30, 55, 25, 0.55);
    transform: translateY(-2px);
    box-shadow: 0 6px 24px rgba(122,189,110,0.12);
  }
  /* ── Lien discret Salon des Idées ── */
  .salon-lien-wrap {
    padding: 1.6rem 0 0.4rem;
  }
  .salon-lien {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    text-decoration: none;
    opacity: 0.45;
    transition: opacity 0.25s;
  }
  .salon-lien:hover { opacity: 0.85; }
  .salon-lien-dot {
    width: 5px; height: 5px;
    border-radius: 50%;
    background: #9b6fd4;
    flex-shrink: 0;
  }
  .salon-lien-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: #c8b4f0;
    letter-spacing: 0.02em;
  }
  .salon-lien-sub {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.62rem;
    color: rgba(200,180,240,0.45);
    font-style: italic;
    letter-spacing: 0.02em;
  }
  .salon-lien-arrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
    color: #9b6fd4;
    transition: transform 0.2s;
  }
  .salon-lien:hover .salon-lien-arrow { transform: translateX(3px); }

  @media (max-width: 480px) {
    .salon-lien-sub { display: none; }
  }




.socle-fondations-bloc {
  display: flex;
  align-items: baseline;
  gap: 0.75rem;
  text-decoration: none;
  border: 1px solid rgba(232, 160, 80, 0.18);
  background: rgba(62, 38, 16, 0.45);
  border-radius: 12px;
  padding: 1.0rem 1.5rem;
  margin: 0 0 1.8rem;
  position: relative;
  overflow: hidden;
  transition: border-color 0.25s, background 0.25s, transform 0.2s, box-shadow 0.25s;
}
.socle-fondations-bloc::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 3px;
  background: linear-gradient(90deg, #e85d20, #f5a623, #ffe066, #f5a623, #e85d20);
  opacity: 0.55;
  transition: opacity 0.25s;
}
.socle-fondations-bloc:hover {
  border-color: rgba(232, 160, 80, 0.32);
  background: rgba(72, 46, 20, 0.58);
  transform: translateY(-2px);
  box-shadow: 0 8px 28px rgba(20, 8, 0, 0.4);
}
.socle-fondations-bloc:hover::before { opacity: 1; }
.socle-fondations-titre {
  font-family: 'Libre Baskerville', serif;
  font-size: 1.1rem;
  font-weight: 700;
  font-style: italic;
  color: #e8a050;
  letter-spacing: 0.01em;
  flex-shrink: 0;
}
.socle-fondations-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: 0.58rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: rgba(255,255,255,0.16);
  flex: 1;
}
.socle-fondations-cta {
  display: flex;
  align-items: center;
  gap: 0.3rem;
  flex-shrink: 0;
}
.socle-fondations-cta-text {
  font-family: 'JetBrains Mono', monospace;
  font-size: 0.70rem;
  letter-spacing: 0.07em;
  text-transform: uppercase;
  color: rgba(232, 160, 80, 0.55);
  opacity: 0;
  transform: translateX(-4px);
  transition: opacity 0.2s, transform 0.2s;
}
.socle-fondations-bloc:hover .socle-fondations-cta-text {
  opacity: 1;
  transform: translateX(0);
}
.socle-fondations-arrow {
  font-family: 'JetBrains Mono', monospace;
  font-size: 1.4rem;
  color: #e8a050;
  opacity: 0.55;
  transition: opacity 0.2s, transform 0.2s;
}
.socle-fondations-bloc:hover .socle-fondations-arrow {
  opacity: 1;
  transform: translateX(3px);
}



  /* ── Système duo · Granularité Émotionnelle ── */

.outils-system {
  margin: 0 0 1.8rem;
}
.outils-system-head {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-bottom: 0.75rem;
}
.outils-system-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: 0.52rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: rgba(232,160,80,0.5);
}
.outils-system-dot {
  color: rgba(255,255,255,0.12);
  font-size: 0.6rem;
}
.outils-system-name {
  font-family: 'JetBrains Mono', monospace;
  font-size: 0.72rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: rgba(255,255,255,0.25);
  flex: 1;
}
.outils-system-count {
  font-family: 'JetBrains Mono', monospace;
  font-size: 0.5rem;
  color: rgba(255,255,255,0.15);
  letter-spacing: 0.08em;
}

.outils-duo {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: stretch;
  background: linear-gradient(135deg, rgba(245,166,35,0.05) 0%, rgba(232,144,106,0.05) 100%);
  border: 1px solid rgba(232,160,80,0.12);
  border-radius: 12px;
  overflow: hidden;
}

.outil-duo-bloc {
  display: flex;
  flex-direction: column;
  padding: 1.2rem 1.4rem;
  text-decoration: none;
  position: relative;
  overflow: hidden;
  transition: background 0.22s;
}
.outil-duo-bloc:hover {
  background: rgba(255,255,255,0.04);
}
.outil-duo-bloc::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 2px;
  opacity: 0.3;
  transition: opacity 0.22s;
}
.outil-duo-bloc:hover::before { opacity: 0.75; }

/* Évaluer — amber/or */
.outil-duo-bloc--evaluer {
  border-right: 1px solid rgba(255,255,255,0.06);
}
.outil-duo-bloc--evaluer::before {
  background: linear-gradient(90deg, #e8a050, #ffe066, #e85d20);
}
.outil-duo-bloc--evaluer .outil-duo-step  { color: #f5a623; }
.outil-duo-bloc--evaluer .outil-duo-titre { color: #f5c460; }
.outil-duo-bloc--evaluer .outil-duo-arrow { color: #f5a623; }

/* Entraîner — corail pêche */
.outil-duo-bloc--entrainer::before {
  background: linear-gradient(90deg, #e8906a, #f0b898, #d4623a);
}
.outil-duo-bloc--entrainer .outil-duo-step  { color: #e8906a; }
.outil-duo-bloc--entrainer .outil-duo-titre { color: #f0a882; }
.outil-duo-bloc--entrainer .outil-duo-arrow { color: #e8906a; }

.outil-duo-step {
  font-family: 'JetBrains Mono', monospace;
  font-size: 0.5rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  margin-bottom: 0.5rem;
  opacity: 0.75;
}
.outil-duo-titre {
  font-family: 'Libre Baskerville', serif;
  font-size: 0.95rem;
  font-weight: 700;
  font-style: italic;
  letter-spacing: 0.01em;
  margin-bottom: 0.5rem;
}
.outil-duo-desc {
  font-family: 'Mulish', sans-serif;
  font-weight: 300;
  font-size: 0.76rem;
  line-height: 1.55;
  color: rgba(220,190,150,0.5);
  flex: 1;
  margin: 0 0 0.75rem;
}
.outil-duo-arrow {
  font-family: 'JetBrains Mono', monospace;
  font-size: 0.9rem;
  opacity: 0.35;
  align-self: flex-end;
  transition: opacity 0.2s, transform 0.2s;
}
.outil-duo-bloc:hover .outil-duo-arrow {
  opacity: 1;
  transform: translateX(3px);
}

/* Glow de fond au hover */
.outil-duo-glow {
  position: absolute;
  bottom: -20px; right: -20px;
  width: 100px; height: 100px;
  border-radius: 50%;
  filter: blur(32px);
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.3s;
}
.outil-duo-bloc:hover .outil-duo-glow { opacity: 0.12; }
.outil-duo-glow--evaluer   { background: #f5a623; }
.outil-duo-glow--entrainer { background: #e8906a; }

/* Connecteur central */
.outil-duo-connector {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 0 0.6rem;
  gap: 0.25rem;
}
.outil-duo-connector-line {
  width: 1px;
  flex: 1;
  max-height: 36px;
  background: linear-gradient(to bottom, rgba(245,166,35,0.18), rgba(232,144,106,0.18));
}
.outil-duo-connector-icon {
  font-family: 'JetBrains Mono', monospace;
  font-size: 0.7rem;
  color: rgba(255,255,255,0.18);
}

/* Mobile : empilé */
@media (max-width: 580px) {
  .outils-duo {
    grid-template-columns: 1fr;
  }
  .outil-duo-bloc--evaluer {
    border-right: none;
    border-bottom: 1px solid rgba(255,255,255,0.06);
  }
  .outil-duo-connector {
    flex-direction: row;
    padding: 0.55rem 1.4rem;
  }
  .outil-duo-connector-line {
    flex: 1;
    height: 1px;
    width: auto;
    max-height: unset;
    background: linear-gradient(to right, rgba(245,166,35,0.18), rgba(232,144,106,0.18));
  }
}
@media (max-width: 480px) {
  .outil-duo-titre { font-size: 0.85rem; }
  .outil-duo-desc  { font-size: 0.74rem; }
}
  
</style>
