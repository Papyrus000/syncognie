<section class="profile-phase" aria-labelledby="profile-phase-title">
  <div class="profile-phase__inner">
    <div class="profile-phase__visual" aria-hidden="true">
      <span class="profile-phase__orb profile-phase__orb--main"></span>
      <span class="profile-phase__orb profile-phase__orb--lower"></span>
      <span class="profile-phase__sparkles"></span>
      <img id="profile-femme" src="/images/femme_v1.png" alt="" />
    </div>

    <div class="profile-phase__content">
      <span class="profile-phase__eyebrow">
        EXPÉRIENCE · <span class="phase-construction">PHASE 1</span>
      </span>

      <h2 id="profile-phase-title">Un parcours personnalisé</h2>

      <p>
        Choisis le parcours qui te convient : exploration rapide,
        en suivant un besoin identifié ou par un profil finement
        paramétré !
      </p>

      <div class="profile-phase__note">
        <span aria-hidden="true">✦</span>
        <em>Un parcours pour chaque niveau de disponibilité.</em>
      </div>

      <div class="profile-phase__link-wrapper">
        <a href="/entree" class="profile-phase__link" aria-label="Démarrer le parcours personnalisé">
          <span class="profile-phase__link-aura" aria-hidden="true"></span>
          <span class="profile-phase__link-label">Démarrer</span>
          <span class="profile-phase__link-icon" aria-hidden="true">→</span>
        </a>
      </div>
    </div>
  </div>
</section>

<script>
  const TOTAL = 20;
  const img = document.getElementById('profile-femme') as HTMLImageElement | null;
  if (img) {
    const n = Math.floor(Math.random() * TOTAL) + 1;
    img.src = `/images/femme_v${n}.png`;
  }
</script>

<style>
.profile-phase {
  --cream: #fff8ef;
  --cream-2: #fff1df;
  --gold: #f0c36a;
  --orange: #ff7a2f;
  --green: #20d45f;
  --green-deep: #06ad46;
  --green-dark: #07813d;
  --ink: rgba(25, 23, 22, 0.9);
  --muted: rgba(77, 72, 68, 0.62);

  position: relative;
  isolation: isolate;
  padding: clamp(0.2rem, 0.9vw, 0.55rem) 2rem clamp(1.25rem, 2.4vw, 2.3rem);
  overflow: visible;
}

.profile-phase::before {
  content: "";
  position: absolute;
  left: max(-4rem, -5vw);
  top: -8%;
  width: min(47vw, 560px);
  height: min(44vw, 520px);
  z-index: -2;
  pointer-events: none;
  border-radius: 49% 51% 53% 47% / 45% 43% 57% 55%;
  background:
    radial-gradient(circle at 34% 18%, rgba(255,255,255,0.72), transparent 23%),
    linear-gradient(146deg, rgba(255, 213, 151, 0.34), rgba(246, 184, 94, 0.13) 62%, transparent 100%);
  box-shadow:
    inset 0 0 0 1px rgba(255,255,255,0.42),
    0 36px 90px rgba(130, 83, 39, 0.085);
}

.profile-phase::after {
  content: "";
  position: absolute;
  left: clamp(10rem, 27vw, 30rem);
  bottom: 8%;
  width: min(32vw, 390px);
  height: min(19vw, 250px);
  z-index: -2;
  pointer-events: none;
  border-radius: 999px;
  opacity: .48;
  transform: rotate(18deg);
  background: linear-gradient(145deg, rgba(255, 220, 166, .30), rgba(255, 240, 210, .10));
  box-shadow: inset 0 0 0 1px rgba(255,255,255,.36);
}

@media (max-width: 720px) {
  .profile-phase::after {
    display: none;
  }
}

.profile-phase__inner {
  transform: translateX(110px);
  width: min(100%, 940px);
  margin: 0 auto;
  display: grid;
  grid-template-columns: minmax(210px, 300px) minmax(0, 1fr);
  align-items: center;
  gap: clamp(1.6rem, 3.8vw, 3.4rem);
}

@media (max-width: 860px) {
  .profile-phase__inner {
    transform: none;
  }
}

.profile-phase__visual {
  position: relative;
  justify-self: center;
  width: min(28vw, 280px);
  min-width: 210px;
  transform: translateX(0.85rem);
}

.profile-phase__orb {
  position: absolute;
  display: block;
  pointer-events: none;
  border-radius: 50%;
  background:
    radial-gradient(circle at 38% 20%, rgba(255,255,255,0.62), transparent 24%),
    linear-gradient(145deg, rgba(253, 207, 138, 0.32), rgba(246, 182, 92, 0.12));
  box-shadow:
    inset 0 0 0 1px rgba(255,255,255,0.42),
    0 30px 76px rgba(144, 94, 42, 0.10);
  filter: saturate(1.06);
}

.profile-phase__orb--main {
  width: 124%;
  aspect-ratio: 1 / 1;
  height: auto;
  left: -20%;
  top: -2%;
}

.profile-phase__orb--lower {
  width: 100%;
  aspect-ratio: 1 / 1;
  height: auto;
  right: -31%;
  bottom: 16%;
  opacity: 0.54;
  transform: rotate(18deg);
}

.profile-phase__visual::before {
  content: "";
  position: absolute;
  left: -13%;
  bottom: 3%;
  width: 45%;
  height: 84%;
  border-radius: 60% 40% 40% 60% / 50% 46% 54% 50%;
  background: linear-gradient(96deg, rgba(55, 48, 43, 0.19), rgba(80, 65, 53, 0.075) 48%, transparent 82%);
  filter: blur(13px);
  transform: skewY(-7deg);
  opacity: 0.72;
}

.profile-phase__visual::after {
  content: "";
  position: absolute;
  left: 9%;
  bottom: 1.5%;
  width: 52%;
  height: 6%;
  border-radius: 999px;
  background: radial-gradient(ellipse, rgba(42, 36, 32, 0.27), rgba(42, 36, 32, 0.08) 56%, transparent 74%);
  filter: blur(9px);
  transform: rotate(-4deg);
  opacity: 0.68;
}

.profile-phase__sparkles {
  position: absolute;
  inset: 6% -20% 14% 22%;
  pointer-events: none;
  opacity: 0.72;
  background:
    radial-gradient(circle at 20% 12%, rgba(255,255,255,0.95) 0 2px, transparent 3px),
    radial-gradient(circle at 44% 24%, rgba(255,255,255,0.88) 0 1.5px, transparent 2.5px),
    radial-gradient(circle at 76% 16%, rgba(255,255,255,0.9) 0 2.5px, transparent 4px),
    radial-gradient(circle at 66% 54%, rgba(255,255,255,0.86) 0 1.5px, transparent 2.5px),
    radial-gradient(circle at 35% 70%, rgba(255,255,255,0.9) 0 2px, transparent 3px),
    radial-gradient(circle at 82% 80%, rgba(255,255,255,0.75) 0 1.5px, transparent 2.5px);
  filter: drop-shadow(0 0 10px rgba(255, 220, 154, 0.85));
}

.profile-phase__visual img {
  position: relative;
  z-index: 2;
  display: block;
  width: 85%;
  margin: 0;
  opacity: 0.99;
  filter:
    saturate(1.1)
    contrast(1.035)
    drop-shadow(-26px 30px 17px rgba(70, 52, 39, 0.18))
    drop-shadow(0 18px 28px rgba(43, 31, 24, 0.11));
  transform: translateZ(0);
}

.profile-phase__content {
  position: relative;
  z-index: 2;
  max-width: 520px;
  padding-top: 0.2rem;
}

.profile-phase .profile-phase__eyebrow {
  display: inline-block;
  margin-bottom: 0.42rem;
  font-family: 'Mulish', sans-serif;
  font-size: 0.58rem;
  font-weight: 800;
  letter-spacing: 0.29em;
  text-transform: uppercase;
  color: rgba(134, 93, 66, 0.64);
}

.phase-construction {
  color: var(--orange);
  text-shadow: 0 0 14px rgba(255, 122, 47, 0.26);
}

.profile-phase .profile-phase__content h2 {
  max-width: 18ch;
  margin: 0 0 clamp(1rem, 2vw, 1.35rem);
  font-family: 'Libre Baskerville', serif;
  font-size: 1.10rem;
  font-weight: 700;
  line-height: 1.12;
  letter-spacing: -0.035em;
  color: var(--ink);
  text-wrap: balance;
}

.profile-phase .profile-phase__content p {
  max-width: 43ch;
  margin: 0 0 clamp(1.1rem, 2vw, 1.65rem);
  font-family: 'Mulish', sans-serif;
  font-size: clamp(0.6rem, 1.2vw, 1.05rem);
  line-height: 1.88;
  color: var(--muted);
}

.profile-phase__note {
  display: flex;
  align-items: flex-start;
  gap: 0.65rem;
  width: min(100%, 390px);
  margin-top: 0.15rem;
  padding: 1rem 1.22rem;
  border-radius: 0.42rem;
  border-left: 3px solid rgba(241, 190, 82, 0.44);
  background:
    linear-gradient(135deg, rgba(255, 243, 219, 0.72), rgba(255, 250, 241, 0.42)),
    radial-gradient(circle at 8% 0%, rgba(255,255,255,0.9), transparent 38%);
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.72),
    0 18px 38px rgba(121, 82, 35, 0.045);
  font-family: 'Libre Baskerville', serif;
  font-size: clamp(0.65rem, 1.0vw, 0.85rem);
  line-height: 1.75;
  color: rgba(85, 82, 78, 0.76);
}

.profile-phase__note span {
  color: rgba(87, 91, 91, 0.8);
  font-size: 1rem;
  line-height: 1.75;
}

.profile-phase__link-wrapper {
  margin-top: clamp(1.35rem, 2.6vw, 2.05rem);
}

.profile-phase__link {
  position: relative;
  isolation: isolate;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.55rem;
  min-height: 2.08rem;
  padding: 0.46rem 0.6rem 0.46rem 0.98rem;
  border: 0;
  border-radius: 999px;
  overflow: visible;
  background:
    radial-gradient(circle at 30% 8%, rgba(255,255,255,0.48), transparent 24%),
    linear-gradient(180deg, #35e46d 0%, var(--green) 48%, var(--green-deep) 100%);
  color: white;
  font-family: 'Mulish', sans-serif;
  font-size: 0.68rem;
  font-weight: 900;
  letter-spacing: 0.13em;
  text-decoration: none;
  text-transform: uppercase;
  text-shadow: 0 1px 1px rgba(0, 93, 42, 0.18);
  box-shadow:
    0 16px 24px rgba(8, 165, 70, 0.22),
    0 6px 9px rgba(6, 129, 61, 0.18),
    0 24px 34px rgba(53, 228, 109, 0.12),
    inset 0 1px 0 rgba(255,255,255,0.42),
    inset 0 -8px 14px rgba(0, 116, 47, 0.12);
  cursor: pointer;
  transform: translateZ(0);
  transition: transform 0.24s ease, box-shadow 0.24s ease, filter 0.24s ease;
}

.profile-phase__link::before {
  content: "";
  position: absolute;
  inset: 1px 1px auto;
  height: 48%;
  z-index: -1;
  border-radius: inherit;
  background: linear-gradient(180deg, rgba(255,255,255,0.34), transparent);
  pointer-events: none;
}

.profile-phase__link::after {
  content: "";
  position: absolute;
  top: -55%;
  bottom: -55%;
  left: -45%;
  width: 40%;
  border-radius: inherit;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.46), transparent);
  transform: rotate(18deg) translateX(-150%);
  transition: transform 0.62s ease;
  pointer-events: none;
}

.profile-phase__link-aura {
  position: absolute;
  inset: 33% 7% -62%;
  z-index: -2;
  border-radius: 999px;
  background: radial-gradient(ellipse, rgba(37, 227, 105, 0.58), rgba(37, 227, 105, 0.16) 44%, transparent 72%);
  filter: blur(13px);
  pointer-events: none;
}

.profile-phase__link-label,
.profile-phase__link-icon {
  position: relative;
  z-index: 1;
}

.profile-phase__link-icon {
  display: inline-grid;
  place-items: center;
  width: 1.05rem;
  height: 1.05rem;
  border-radius: 50%;
  background:
    radial-gradient(circle at 35% 22%, rgba(255,255,255,0.58), transparent 32%),
    rgba(255,255,255,0.18);
  color: #fff;
  font-size: 0.74rem;
  line-height: 1;
  box-shadow:
    inset 0 0 0 1px rgba(255,255,255,0.20),
    0 3px 8px rgba(0, 113, 48, 0.12);
  transition: transform 0.24s ease, background 0.24s ease;
}

.profile-phase__link:hover {
  transform: translateY(-1px);
  filter: saturate(1.08);
  box-shadow:
    0 19px 30px rgba(8, 165, 70, 0.28),
    0 8px 12px rgba(6, 129, 61, 0.18),
    0 28px 42px rgba(53, 228, 109, 0.16),
    inset 0 1px 0 rgba(255,255,255,0.5),
    inset 0 -8px 14px rgba(0, 116, 47, 0.12);
}

.profile-phase__link:hover::after {
  transform: rotate(18deg) translateX(360%);
}

.profile-phase__link:hover .profile-phase__link-icon {
  transform: translateX(2px);
  background: rgba(255,255,255,0.24);
}

.profile-phase__link:focus-visible {
  outline: none;
  box-shadow:
    0 0 0 3px rgba(255,255,255,0.95),
    0 0 0 5px rgba(32, 212, 95, 0.42),
    0 19px 30px rgba(8, 165, 70, 0.28),
    inset 0 1px 0 rgba(255,255,255,0.5);
}

@media (max-width: 720px) {
  .profile-phase {
    padding-inline: 1.2rem;
  }

  .profile-phase__inner {
    grid-template-columns: 1fr;
    gap: 1.6rem;
    text-align: center;
    transform: none;
  }

  .profile-phase__visual {
    width: min(70vw, 270px);
    min-width: 0;
    transform: none;
    margin: 0 auto;
  }

  .profile-phase__content {
    justify-self: center;
  }

  .profile-phase .profile-phase__content h2,
  .profile-phase .profile-phase__content p,
  .profile-phase__note {
    margin-left: auto;
    margin-right: auto;
  }

  .profile-phase__note {
    text-align: left;
  }
}

@media (max-width: 480px) {
  .profile-phase {
    padding-inline: 1rem;
  }

  .profile-phase__visual {
    width: min(65vw, 220px);
  }

  .profile-phase .profile-phase__content h2 {
    font-size: 1.05rem;
  }

  .profile-phase__link-wrapper {
    display: flex;
    justify-content: center;
  }
}
</style>
