<section class="profile-phase">
  <div class="profile-phase__inner">

    <div class="profile-phase__visual">
      <img src="/images/femme_v1.png" alt="" />
    </div>

    <div class="profile-phase__content">
<span class="profile-phase__eyebrow">
  EXPÉRIENCE · 
  <span class="phase-construction">
    PHASE 1
  </span>
</span>

      <h2>
        Un parcours personnalisé
      </h2>

      <p>
        Choisis le parcours qui te convient : exploration rapide, 
        en suivant un besoin identifié ou par un profil finement 
        paramétré !
      </p>

      <div class="profile-phase__note">
        ✦ Un parcours pour chaque niveau de disponibilité.
      </div>

<div class="profile-phase__link-wrapper">
  <a href="/entree" class="profile-phase__link" aria-label="Démarrer le parcours personnalisé">
    <span class="profile-phase__link-aura" aria-hidden="true"></span>
    <span class="profile-phase__link-label">Démarrer</span>
    <span class="profile-phase__link-icon" aria-hidden="true">→</span>
  </a>
</div>
      
    </div>

  </div>
</section>


<style>
.profile-phase {
  position: relative;
  padding: .5rem 2rem 1rem;
}

.profile-phase__inner {
  max-width: 740px;
  margin: 0 auto;

  display: grid;
  grid-template-columns: 260px 1fr;
  gap: 3.5rem;
align-items: start;
}

.profile-phase__visual {
  max-width: 250px;
  justify-self: center;
  margin-left: 2.0rem;
}

.profile-phase__visual::before {
  content: "";

  position: absolute;
  inset: 10%;

  background:
    radial-gradient(
      circle,
      rgba(224, 179, 92, 0.18),
      transparent 70%
    );

  filter: blur(40px);
  z-index: 0;
}

.profile-phase__visual img {
  width: 100%;
  margin: 0;
  display: block;
  opacity: 0.92;

  filter:
    drop-shadow(0 12px 18px rgba(0,0,0,0.10))
    drop-shadow(0 30px 40px rgba(0,0,0,0.12));

  transform: translateZ(0);

}

.profile-phase .profile-phase__eyebrow {
  font-family: 'Mulish', sans-serif;

  font-size: 0.62rem;

  letter-spacing: 0.24em;

  text-transform: uppercase;

  color: rgba(139, 94, 60, 0.58);

  margin-bottom: 0.6rem;
}

.profile-phase .profile-phase__content p {
  font-family: 'Mulish', sans-serif;

  font-size: 0.88rem;


  line-height: 1.75;

  color: var(--ink-soft);

  max-width: 42ch;

  margin: 0 0 0.65rem;
}

.profile-phase .profile-phase__content h2 {
  font-family: 'Libre Baskerville', serif;

  font-size: 0.95rem;
  font-weight: 700;

  line-height: 1.35;

  letter-spacing: -0.01em;

  color: rgba(26, 23, 20, 0.82);

  margin: 0 0 0.7rem;

  max-width: 22ch;
}

.profile-phase__note {
  margin-top: 1rem;

  padding: 0.55rem 0.8rem;

  max-width: 40ch;

  background: rgba(232,184,50,.05);

  border-left: 2px solid rgba(232,184,50,.18);

  font-family: 'Mulish', sans-serif;

  font-size: 0.76rem;

  font-style: italic;

  line-height: 1.6;

  color: var(--ink-soft);
}

.phase-construction {
  color: #ff8a3d;
  font-weight: 700;
  text-shadow: 0 0 8px rgba(255, 138, 61, 0.35);
}


.profile-phase__link-wrapper {
  margin-top: 1.1rem;
}

.profile-phase__link {
  display: inline-flex;
  align-items: center;
  gap: 0.45rem;

  padding: 0.65rem 1rem;
  border-radius: 999px;

  position: relative;
  isolation: isolate;
  overflow: hidden;

  background:
    linear-gradient(135deg, rgba(255,255,255,0.88), rgba(255,255,255,0.28)) padding-box,
    linear-gradient(135deg, rgba(63, 224, 143, 0.95), rgba(31, 180, 112, 0.72), rgba(255,255,255,0.65)) border-box;
  border: 1px solid transparent;

  color: #168a5c;
  font-family: 'Mulish', sans-serif;
  font-size: 0.68rem;
  font-weight: 800;
  letter-spacing: 0.045em;
  text-decoration: none;
  text-transform: uppercase;

  box-shadow:
    0 10px 26px rgba(19, 155, 95, 0.16),
    0 2px 6px rgba(19, 155, 95, 0.10),
    inset 0 1px 0 rgba(255,255,255,0.78),
    inset 0 -10px 18px rgba(39, 213, 128, 0.10);

  transform: translateZ(0);
  transition:
    color 0.25s ease,
    border-color 0.25s ease,
    box-shadow 0.25s ease,
    transform 0.25s ease;
}

.profile-phase__link::before {
  content: "";
  position: absolute;
  inset: 1px;
  z-index: -2;
  border-radius: inherit;
  background:
    radial-gradient(circle at 24% 0%, rgba(255,255,255,0.95), transparent 34%),
    linear-gradient(135deg, rgba(229, 255, 243, 0.95), rgba(198, 248, 224, 0.72) 48%, rgba(238, 255, 247, 0.86));
}

.profile-phase__link::after {
  content: "";
  position: absolute;
  top: -70%;
  bottom: -70%;
  left: -40%;
  width: 42%;
  z-index: -1;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.72), transparent);
  transform: rotate(18deg) translateX(-145%);
  transition: transform 0.55s ease;
}

.profile-phase__link-aura {
  position: absolute;
  inset: -1px;
  z-index: -3;
  border-radius: inherit;
  background: radial-gradient(circle at 50% 100%, rgba(45, 220, 129, 0.42), transparent 66%);
  opacity: 0.7;
  filter: blur(8px);
}

.profile-phase__link-label,
.profile-phase__link-icon {
  position: relative;
  z-index: 1;
}

.profile-phase__link-icon {
  display: inline-grid;
  place-items: center;
  width: 0.95rem;
  height: 0.95rem;
  border-radius: 50%;
  background: rgba(29, 181, 112, 0.12);
  color: #11905c;
  font-size: 0.72rem;
  line-height: 1;
  box-shadow: inset 0 0 0 1px rgba(22, 138, 92, 0.10);
  transition:
    transform 0.25s ease,
    background 0.25s ease,
    color 0.25s ease;
}

.profile-phase__link:hover {
  color: #08764d;
  transform: translateY(-1px);
  box-shadow:
    0 14px 30px rgba(19, 155, 95, 0.22),
    0 4px 10px rgba(19, 155, 95, 0.13),
    inset 0 1px 0 rgba(255,255,255,0.86),
    inset 0 -10px 18px rgba(39, 213, 128, 0.16);
}

.profile-phase__link:hover::after {
  transform: rotate(18deg) translateX(340%);
}

.profile-phase__link:hover .profile-phase__link-icon {
  transform: translateX(2px);
  background: rgba(24, 175, 105, 0.18);
  color: #08764d;
}

.profile-phase__link:focus-visible {
  outline: none;
  box-shadow:
    0 0 0 3px rgba(255,255,255,0.92),
    0 0 0 5px rgba(39, 213, 128, 0.45),
    0 14px 30px rgba(19, 155, 95, 0.22),
    inset 0 1px 0 rgba(255,255,255,0.86);
}


.profile-phase__content {
  position: relative;
  z-index: 2;
}

.profile-phase__link {
  z-index: 3;
  cursor: pointer;
}
</style>